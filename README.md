# RIZIN × プロテイン餃子｜事業提案LP

RIZIN本社向けの提案用1ページLP（静的サイト）。`index.html` / `styles.css` / `main.js` の3ファイル構成。

**機密資料（CONFIDENTIAL｜株式会社bf2）。**

## ローカル確認

任意の静的サーバ、または `index.html` をブラウザで直接開く。

## Vercelデプロイ手順

このディレクトリで `vercel` を実行（または Vercel ダッシュボードでこのフォルダを Import）すれば、ビルド設定不要でそのまま公開できます。
